//! Single source of truth for program version.
//! Import this from protocol.zig and anywhere else that needs the version.
//! Bump this file to trigger auto-upgrade on all Guests.

pub const VERSION = "0.6.5";
